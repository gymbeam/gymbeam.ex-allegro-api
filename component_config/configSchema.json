{
  "type": "object",
  "title": "extractor configuration",
  "required": [
    "#client_id",
    "#client_secret"
  ],
  "properties": {
    "#client_id": {
      "type": "string",
      "format": "password",
      "title": "Allegro Client ID"
    },
    "#client_secret": {
      "type": "string",
      "format": "password",
      "title": "Allegro Client Secret"
    },
    "endpoint": {
      "title": "Endpoint",
      "type": "string",
      "enum": [
        "Billing entries",
        "endpoint y"
      ]
    }
  }
}
